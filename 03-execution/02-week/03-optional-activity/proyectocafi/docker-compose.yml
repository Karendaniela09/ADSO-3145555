version: '3.8'
services:
postgres:
  image: postgres:15
  container_name: postgres-cafi
  restart: always
  environment:
    POSTGRES_DB: proyectocafi
    POSTGRES_USER: admin
    POSTGRES_PASSWORD: admin123
        s_data:/var/lib/postgresql/data

volumes:
    postgres_data:
